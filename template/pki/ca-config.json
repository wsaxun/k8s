{
  "signing": {
    "default": {
      "expiry":  "{{.Duration}}"
    },
    "profiles": {
      "kubernetes": {
        "usages": [
            "signing",
            "key encipherment",
            "server auth",
            "client auth"
        ],
        "expiry": "{{.Duration}}"
      }
    }
  }
}
